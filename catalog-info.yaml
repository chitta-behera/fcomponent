apiVersion: backstage.io/v1alpha1
kind: Location
metadata:
  name: demo-template-location
  description: A collection of software templates
spec:
  targets:
    - template.yaml  # Make sure this path is correct
